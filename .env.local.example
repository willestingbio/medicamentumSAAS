# Medicamentum360 — Variables de entorno (ejemplo)
# NOTA: Copia a .env.local y completa los valores reales. NUNCA commitees .env.local.

# ===== Database =====
# InsForge cloud NO expone Postgres vía TCP — no hay DATABASE_URL externa en cloud.
# Esta URL solo funciona para Postgres LOCAL de desarrollo.
# Para runtime en cloud, usa createAdminClient({ apiKey }) de @insforge/sdk.
# Si no tienes Postgres local, instala con: sudo apt install postgresql && sudo -u postgres createdb medicamentum360
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/medicamentum360"

# ===== Better Auth =====
# Genera con: openssl rand -base64 32
BETTER_AUTH_SECRET="tu-secret-de-32-caracteres-aqui"
BETTER_AUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ===== Google OAuth =====
# Obtén de: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="tu-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="tu-google-client-secret"

# ===== Wompi (pagos) =====
# Pruebas: ambiente test de Wompi. Producción: credenciales reales.
WOMPI_PUBLIC_KEY="pub_test_..."
WOMPI_PRIVATE_KEY="prv_test_..."
WOMPI_EVENTS_SECRET="tu-wompi-webhook-secret"

# ===== Moodle (entorno LOCAL de pruebas) =====
# Auto-generado por docker/moodle-bootstrap.php
# Después de docker compose up -d en docker/, copia de docker/output/.env.moodle.local:
MOODLE_BASE_URL="http://localhost:8090"
MOODLE_WS_TOKEN="tu-moodle-token"

# ===== InsForge =====
# API key de .insforge/project.json — full-access admin, server-only
INSFORGE_API_KEY="ik_..."
# JWT secret para bridge token (npx @insforge/cli secrets get JWT_SECRET)
INSFORGE_JWT_SECRET="tu-insforge-jwt-secret"
NEXT_PUBLIC_INSFORGE_BASE_URL="https://tu-proyecto.region.insforge.app"
# Anon key (npx @insforge/cli secrets get ANON_KEY)
NEXT_PUBLIC_INSFORGE_ANON_KEY="tu-insforge-anon-key"

# ===== Configuración de marca =====
NEXT_PUBLIC_BRAND_COLOR="#8127cf"
NEXT_PUBLIC_BRAND_NAME="Medicamentum360"

# ===== Logging =====
DEBUG="false"
NODE_ENV="development"
